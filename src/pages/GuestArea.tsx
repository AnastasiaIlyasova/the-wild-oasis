import { GuestAreaWrapper } from "../components/guest/guestAreaWrapper";
import {Container} from "../shared/ui/container/Container";

export const GuestArea = () => {
    return (
        <Container>
            <GuestAreaWrapper/>
        </Container>
    );
}
